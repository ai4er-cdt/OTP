name: gtc
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - ipykernel
  - numpy
  - xarray
  - xgcm
  - xmitgcm
  - pyresample
  - netcdf4
  - matplotlib
  - cartopy
  - tqdm
  - requests
prefix: /home/maish/miniconda3/envs/gtc # change this to the path to your conda envs -- use `conda info --envs` to figure this out
